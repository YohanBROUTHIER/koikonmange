
import { BsClockHistory } from "react-icons/bs";
import style from './index.module.css'

const AlarmTime = () => {

    return(
        <BsClockHistory className={`${style.color} ${style.size}`}/>
    )
}

export default AlarmTime;